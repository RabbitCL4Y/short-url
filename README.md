# short-url
#Tutorial Penginstallan
$pkg update && pkg upgrade
$pkg install curl
$pkg install ruby
$gem install lolcat
$pkg install git
$git clone https://github.com/RabbitCL4Y/short-url
$cd short-url
$sh url.sh
